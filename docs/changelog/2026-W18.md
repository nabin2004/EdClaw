# Weekly Changelog - 2026-W18 (Apr 27 - May 3)

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
This is a per-week supplement; canonical release notes live in [../../CHANGELOG.md](../../CHANGELOG.md).

## Added

- **Site generation package** (`src/educlaw/sitegen/`): `generate_site`, lecture
  conversion, registry, and catalog modules; new `copier` dependency in
  `pyproject.toml`. (`508482e`)
- **Copier Jekyll template** under `templates/course_site/` (with `copier.yml`
  and `.jinja` files) and an HTML course index at `templates/course_catalog/`.
  (`508482e`)
- **CLI: `educlaw site` subcommands** - `generate`, `catalog`, `list` in
  `src/educlaw/cli.py`. (`1edc843`)
- **Pipeline integration**: `--generate-site` and `--site-output-dir` flags on
  `scripts/run_full_course_pipeline.py`. (`1edc843`)
- **Docs**: new `docs/SITE_GENERATION.md`; cross-links from
  `docs/AUTOCOURSE.md`, `docs/DEVELOPERS.md`, and `docs/ROADMAP.md`. (`508482e`, `1edc843`)

## Changed

- **Brand palette - Harvard Crimson** applied to course-site theme:
  primary/header `#A51C30`, accent `#A41034`, titles `#1E1E1E`. Catalog page,
  Copier `theme_color` defaults, home semester subtitle, and archive
  `postlower` links all aligned. (`e4f9995`)

## Repository

- `.gitignore` updated to ignore local `site/` and generated `sites/`. (`1edc843`)

## Commits

- `508482e` feat(sitegen): add Copier Jekyll template, Python package, and docs
- `1edc843` feat(cli): add educlaw site commands and pipeline --generate-site
- `e4f9995` style(course-site): apply Harvard Crimson brand palette
